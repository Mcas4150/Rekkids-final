
<%= simple_form_for [@record, @order] do |f| %>
  <%= @record.id %>
  <%= cl_image_tag @record.photo.path, height: 300, width: 400, crop: :fill %>
  <%= @record.name %>
  <%= @record.artist %>
  <%= @record.price %>
  <%= f.submit "place order", class: "btn btn-primary" %>
<% end %>
